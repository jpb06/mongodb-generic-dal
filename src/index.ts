import {
    clearAllAndCreateMany, clearAndCreateMany, create, createOrUpdate, getAll, getBy, remove
} from "./dal/generic";

export {
  clearAllAndCreateMany,
  clearAndCreateMany,
  create,
  createOrUpdate,
  getAll,
  getBy,
  remove,
};
